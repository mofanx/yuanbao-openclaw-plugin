/**
 * Custom command loader.
 *
 * Importing this file triggers side-effect registration of all custom commands.
 * Keep every command module listed here so it is loaded at runtime.
 */

import "./commands/model.js";
import "./commands/models.js";
